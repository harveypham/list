{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Test",
            "type": "shell",
            "command": "g++ -g test.cpp -o test.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}